/**
 * Generated by orval from TallyCheck Corporate API v2.1.0.
 * DO NOT EDIT — run `nx run api-client:generate` instead.
 */
import {
  HttpClient,
  HttpHeaders,
  HttpResponse as AngularHttpResponse,
} from "@angular/common/http";
import type { HttpContext, HttpEvent, HttpParams } from "@angular/common/http";

import { Injectable, inject } from "@angular/core";

import { Observable } from "rxjs";

import { tcheckApiBase } from "../../../base-url";

import type {
  GetPlatformOrganizations200,
  GetPlatformOrganizationsOrgIdDashboard200,
  GetPlatformSummary200,
} from "../model";

interface HttpClientOptions {
  readonly headers?: HttpHeaders | Record<string, string | string[]>;
  readonly context?: HttpContext;
  readonly params?:
    | HttpParams
    | Record<
        string,
        string | number | boolean | Array<string | number | boolean>
      >;
  readonly reportProgress?: boolean;
  readonly withCredentials?: boolean;
  readonly credentials?: RequestCredentials;
  readonly keepalive?: boolean;
  readonly priority?: RequestPriority;
  readonly cache?: RequestCache;
  readonly mode?: RequestMode;
  readonly redirect?: RequestRedirect;
  readonly referrer?: string;
  readonly integrity?: string;
  readonly referrerPolicy?: ReferrerPolicy;
  readonly transferCache?: { includeHeaders?: string[] } | boolean;
  readonly timeout?: number;
}

type HttpClientBodyOptions = HttpClientOptions & {
  readonly observe?: "body";
};

type HttpClientEventOptions = HttpClientOptions & {
  readonly observe: "events";
};

type HttpClientResponseOptions = HttpClientOptions & {
  readonly observe: "response";
};

type HttpClientObserveOptions = HttpClientOptions & {
  readonly observe?: "body" | "events" | "response";
};

@Injectable({ providedIn: "root" })
export class PlatformService {
  private readonly http = inject(HttpClient);
  /**
   * Super-admin only. Aggregates each active tenant's dashboard KPIs; per-org detail is returned in the envelope `meta.organizations`. A tenant that cannot be read is reported with an `error` instead of `kpis` rather than failing the whole response.
   * @summary Platform-wide attendance totals across all active tenants
   */
  getPlatformSummary<TData = GetPlatformSummary200>(
    options?: HttpClientBodyOptions,
  ): Observable<TData>;
  getPlatformSummary<TData = GetPlatformSummary200>(
    options?: HttpClientEventOptions,
  ): Observable<HttpEvent<TData>>;
  getPlatformSummary<TData = GetPlatformSummary200>(
    options?: HttpClientResponseOptions,
  ): Observable<AngularHttpResponse<TData>>;
  getPlatformSummary<TData = GetPlatformSummary200>(
    options?: HttpClientObserveOptions,
  ): Observable<TData | HttpEvent<TData> | AngularHttpResponse<TData>> {
    if (options?.observe === "events") {
      return this.http.get<TData>(`${tcheckApiBase()}/platform/summary`, {
        ...(options as Omit<NonNullable<typeof options>, "observe">),
        observe: "events",
      });
    }

    if (options?.observe === "response") {
      return this.http.get<TData>(`${tcheckApiBase()}/platform/summary`, {
        ...(options as Omit<NonNullable<typeof options>, "observe">),
        observe: "response",
      });
    }

    return this.http.get<TData>(`${tcheckApiBase()}/platform/summary`, {
      ...(options as Omit<NonNullable<typeof options>, "observe">),
      observe: "body",
    });
  }
  /**
   * Super-admin only. Includes suspended orgs; headline stats are null for those.
   * @summary All organizations with a headline stat each
   */
  getPlatformOrganizations<TData = GetPlatformOrganizations200>(
    options?: HttpClientBodyOptions,
  ): Observable<TData>;
  getPlatformOrganizations<TData = GetPlatformOrganizations200>(
    options?: HttpClientEventOptions,
  ): Observable<HttpEvent<TData>>;
  getPlatformOrganizations<TData = GetPlatformOrganizations200>(
    options?: HttpClientResponseOptions,
  ): Observable<AngularHttpResponse<TData>>;
  getPlatformOrganizations<TData = GetPlatformOrganizations200>(
    options?: HttpClientObserveOptions,
  ): Observable<TData | HttpEvent<TData> | AngularHttpResponse<TData>> {
    if (options?.observe === "events") {
      return this.http.get<TData>(`${tcheckApiBase()}/platform/organizations`, {
        ...(options as Omit<NonNullable<typeof options>, "observe">),
        observe: "events",
      });
    }

    if (options?.observe === "response") {
      return this.http.get<TData>(`${tcheckApiBase()}/platform/organizations`, {
        ...(options as Omit<NonNullable<typeof options>, "observe">),
        observe: "response",
      });
    }

    return this.http.get<TData>(`${tcheckApiBase()}/platform/organizations`, {
      ...(options as Omit<NonNullable<typeof options>, "observe">),
      observe: "body",
    });
  }
  /**
   * Super-admin only. Runs the identical dashboard query the tenant's own admins see, against that tenant's schema.
   * @summary One organization's dashboard, viewed by a super-admin
   */
  getPlatformOrganizationsOrgIdDashboard<
    TData = GetPlatformOrganizationsOrgIdDashboard200,
  >(orgId: string, options?: HttpClientBodyOptions): Observable<TData>;
  getPlatformOrganizationsOrgIdDashboard<
    TData = GetPlatformOrganizationsOrgIdDashboard200,
  >(
    orgId: string,
    options?: HttpClientEventOptions,
  ): Observable<HttpEvent<TData>>;
  getPlatformOrganizationsOrgIdDashboard<
    TData = GetPlatformOrganizationsOrgIdDashboard200,
  >(
    orgId: string,
    options?: HttpClientResponseOptions,
  ): Observable<AngularHttpResponse<TData>>;
  getPlatformOrganizationsOrgIdDashboard<
    TData = GetPlatformOrganizationsOrgIdDashboard200,
  >(
    orgId: string,
    options?: HttpClientObserveOptions,
  ): Observable<TData | HttpEvent<TData> | AngularHttpResponse<TData>> {
    if (options?.observe === "events") {
      return this.http.get<TData>(
        `${tcheckApiBase()}/platform/organizations/${orgId}/dashboard`,
        {
          ...(options as Omit<NonNullable<typeof options>, "observe">),
          observe: "events",
        },
      );
    }

    if (options?.observe === "response") {
      return this.http.get<TData>(
        `${tcheckApiBase()}/platform/organizations/${orgId}/dashboard`,
        {
          ...(options as Omit<NonNullable<typeof options>, "observe">),
          observe: "response",
        },
      );
    }

    return this.http.get<TData>(
      `${tcheckApiBase()}/platform/organizations/${orgId}/dashboard`,
      {
        ...(options as Omit<NonNullable<typeof options>, "observe">),
        observe: "body",
      },
    );
  }
}
