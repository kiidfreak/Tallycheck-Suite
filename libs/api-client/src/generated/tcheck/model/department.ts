/**
 * Generated by orval from TallyCheck Corporate API v2.1.0.
 * DO NOT EDIT — run `nx run api-client:generate` instead.
 */
import type { UnitType } from "./unitType";

/**
 * A node in the tenant org tree. Despite the name this covers every level — regions, sites, divisions, departments, teams — discriminated by `unit_type`.
 */
export interface Department {
  id?: number;
  /** Display text. `display_name` when one was set, otherwise the slug prettified. Prefer this for rendering. */
  name?: string;
  /** Stored identifier, and the uniqueness key. Unique among siblings, not tenant-wide, so two sites may each have a "reception". */
  slug?: string;
  /**
   * Human-authored label. Exists because the slug round-trip is lossy — "iOS Team" stores as "ios_team" and reads back as "Ios Team".
   * @nullable
   */
  display_name?: string | null;
  unit_type?: UnitType;
  /** @nullable */
  manager_id?: number | null;
  /** @nullable */
  manager_name?: string | null;
  employee_count?: number;
  /** Immediate children only, not the whole subtree. */
  child_count?: number;
  /** @nullable */
  parent_department_id?: number | null;
  /** @nullable */
  parent_department_name?: string | null;
}
