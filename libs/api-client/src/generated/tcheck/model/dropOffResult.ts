/**
 * Generated by orval from TallyCheck Corporate API v2.1.0.
 * DO NOT EDIT — run `nx run api-client:generate` instead.
 */
import type { Timestamp } from "./timestamp";
import type { Uuid } from "./uuid";

export interface DropOffResult {
  token_id?: Uuid;
  child_name?: string;
  /** Four digits. Returned exactly once, at issue time — it is not retrievable afterwards, so the caller must surface it immediately. */
  pin?: string;
  /** Opaque single-use string encoded into the QR ticket. */
  qr_payload?: string;
  expires_at?: Timestamp;
}
